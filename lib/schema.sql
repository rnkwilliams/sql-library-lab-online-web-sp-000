

CREATE TABLE characters(
  id INTEGER PRIMARY KEY,
  name TEXT,
  motto TEXT,
  species TEXT
  );

CREATE TABLE books(
  id INTEGER PRIMARY KEY,
);



CREATE TABLE authors(
  id INTEGER PRIMARY KEY,
);

CREATE TABLE sub-genres(
  id INTEGER PRIMARY KEY,
);

